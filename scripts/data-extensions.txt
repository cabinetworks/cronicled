# Extensions the leak guard treats as data files (scripts/check_leaks,
# check_file_types). Every glob here must also appear in .gitignore's deny-by-default
# block, and vice versa; tests/test_guard.py asserts the two agree in both
# directions. One glob per line, each shaped like *.ext - the guard aborts on
# a line that survives comment/blank stripping but does not look like a glob.
*.json
*.jsonl
*.db
*.db-journal
*.sqlite
*.sqlite3
*.tsv
*.csv
*.log
*.err
*.pid
*.key
*.pem
*.crt
*.env
*.sql
*.bak
*.nfo
*.yaml
*.yml
*.ini
*.cfg
*.toml
*.zip
*.tar
*.gz
*.rar
*.parquet
